#/bin/bash

# small script to set default output device
case $1 in

	"headphones")
	sink=$(pactl list sinks short | grep "Kingston_HyperX_7.1_Audio" | awk '{print $1}')
	;;
	"speakers")
	sink=$(pactl list sinks short | grep "pci" | awk '{print $1}')
	;;

esac

pactl set-default-sink $sink

