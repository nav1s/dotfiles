#/bin/bash

# small script to set default output device
case $1 in

	"headphones")
	sink=$(pactl list sinks short | grep "bluez_output" | awk '{print $1}')
	;;
	"speakers")
	sink=$(pactl list sinks short | grep "6.analog-stereo" | awk '{print $1}')
	;;

esac

pactl set-default-sink $sink

