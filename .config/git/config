[init]
	defaultBranch = main

# setup delta
[core]
    pager = delta --dark
	excludesfile = /home/work/.gitignore_global
	autocrlf = input

[interactive]
    diffFilter = delta --color-only --dark

[delta]
    navigate = true  # use n and N to move between diff sections
    light = false    # set to true if you're in a terminal w/ a light background color (e.g. the default macOS terminal)

[merge]
    conflictstyle = diff3

[diff]
    colorMoved = default
[user]
    name = nav1s
    email = 42621369+nav1s@users.noreply.github.com
    signingkey = 541D97C7ABC07F39 
[commit]
    gpgsign = true
